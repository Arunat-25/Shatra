[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: full-stack tests with Redis and PostgreSQL
addopts =
    -ra
    --tb=short
    -v
    --no-header
